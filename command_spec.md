# ステージコントローラ コマンド仕様

**対象:** stage_controller_firm_v0 / v1（コマンド仕様は共通。差異はピン/タイマ割当のみ）  
**更新日:** 2026-08-09

---

## 1. 通信仕様

| 項目 | 値 |
|------|-----|
| インタフェース | USB CDC (Virtual COM Port) |
| 終端文字（送信）| `\n` (LF, 0x0A) |
| 終端文字（受信）| `\n` (LF, 0x0A) |
| エンコーディング | ASCII |
| コマンドチェーン区切り | `;` |

コマンドは 1 行 1 コマンド（またはチェーン）を送信し、ファームウェアは 1 行の応答を返す。

---

## 2. 軸番号

| 番号 | 別名 | 説明 |
|------|------|------|
| `0` | `X` | 軸 0 (X) |
| `1` | `Y` | 軸 1 (Y) |
| `2` | `Z` | 軸 2 (Z) |

GUI の `axis_no()` 関数により `X`/`Y`/`Z` は `0`/`1`/`2` に変換される。ファームウェアは数字のみを受け付ける。

### 2.1 カスケード接続時のアドレッシング（`@<box>` ルーティング）

複数コントローラをカスケード接続する場合、各機は**機番 `BOX_NO`** を持つ（マスター=0 慣例）。
PC はマスター機に対し、コマンド行の先頭に **`@<box>;`** を付けて対象機を指定する。

| 形式 | 説明 |
|------|------|
| `M0P` | 従来互換。先頭に `@` が無ければ自機（マスター）ローカル軸として実行 |
| `@0;M0P` | 機番0（マスター）の軸0の位置。マスターがローカル実行 |
| `@1;M0P` | 機番1（スレーブ）の軸0の位置。マスターがカスケードSPIで機番1へ中継し応答を返す |
| `@1;MS5000;M2R1000` | 機番1へ複数サブコマンド（`;`区切り）をまとめて中継 |

- **各機内の軸番号は 0..2 のローカル番号**（`@<box>` で機体を選ぶ）。GUI 上は「グローバル軸番号」
  （0..N-1）で扱い、`box = g÷3, ローカル軸 = g%3` に換算して `@<box>;` を付与する。
- 中継先が応答しない/CRC不一致のときマスターは `NG` を返す（ハングしない）。
- スレーブ機が自機宛でない `@<box>` を受けた場合は `NG`。

---

## 3. 応答フォーマット

| 種類 | 応答 | 説明 |
|------|------|------|
| 成功（値なし）| `OK\n` | 動作コマンド全般 |
| 成功（値あり）| `<値>\n` | クエリコマンド全般 |
| 複数行応答終端 | `END\n` | `RA` コマンドの最終行 |
| 実行不可（動作中）| `BUSY\n` | `RP`（パラメータ再適用）を軸動作中に送った場合 |
| エラー | `NG\n` | パラメータ異常・実行不可 |

---

## 4. コマンド一覧

### 4.1 速度設定コマンド

速度コマンドは通常、移動コマンドとセミコロンでチェーンして使用する。

| コマンド | 方向 | 説明 | 応答例 |
|----------|------|------|--------|
| `MS<freq>` | → | パルス駆動周波数設定 [pps] | `OK` |
| `MSR` | ← | パルス駆動周波数読み出し | `1000` |
| `mS<speed>` | → | 物理量駆動速度設定 [mm/s 等] | `OK` |
| `mSR` | ← | 物理量駆動速度読み出し | `1.5` |

### 4.2 パルス量ベースの移動コマンド（`M` プレフィックス）

| コマンド | 説明 | 応答 | 備考 |
|----------|------|------|------|
| `MS<spd>;M<n>R<pls>` | 相対移動（即時応答）| `OK` | 大文字 R = 完了を待たず返答 |
| `MS<spd>;M<n>r<pls>` | 相対移動（完了待ち）| `OK` | 小文字 r = 完了後に返答 |
| `MS<spd>;M<n>A<pls>` | 絶対移動（即時応答）| `OK` | 大文字 A |
| `MS<spd>;M<n>a<pls>` | 絶対移動（完了待ち）| `OK` | 小文字 a |
| `M<n>P` | 現在位置読み出し [パルス] | `<pos>` | 例: `1234` |
| `M<n>L` | センサ状態診断 | `ORG=x CW=x CCW=x rawH ORG=x CW=x CCW=x` | 前半=論理active(検出=1)、rawH以降=生ピンレベル(High=1) |
| `M<n>I` | ホーミング（即時応答）| `OK` | |
| `M<n>i` | ホーミング（完了待ち）| `OK` | |
| `M<n>NP` | JOG 正方向（CW）開始 | `OK` | |
| `M<n>NM` | JOG 負方向（CCW）開始 | `OK` | |
| `M<n>F` | JOG 停止 | `OK` | 停止時の位置は出力済みパルスから確定 |

- `<n>`: 軸番号 (0/1/2)
- `<pls>`: パルス数（正 = CW 方向、負 = CCW 方向）
- `<spd>`: 速度 [pps]

**送信例:**

```
MS1000;M0R500\n     → 軸0を+500パルス相対移動（即時）
MS500;M1r-200\n     → 軸1を-200パルス相対移動（完了待ち）
MS2000;M2A10000\n   → 軸2を絶対位置10000へ移動（即時）
M0P\n               → 軸0の現在位置を取得
M2NP\n              → 軸2のJOG正方向開始
M2F\n               → 軸2のJOG停止
M1i\n               → 軸1のホーミング（完了待ち）
```

### 4.3 物理量ベースの移動コマンド（`m` プレフィックス）

`M` を小文字 `m` にすると物理量（mm 等）での指定になる。内部でパルス数へ変換される（係数 = `P_motor_coeff[n]`）。

| コマンド | 説明 | 応答 |
|----------|------|------|
| `mS<spd>;m<n>R<dist>` | 相対移動（即時）[mm] | `OK` |
| `mS<spd>;m<n>r<dist>` | 相対移動（完了待ち）[mm] | `OK` |
| `mS<spd>;m<n>A<pos>` | 絶対移動（即時）[mm] | `OK` |
| `mS<spd>;m<n>a<pos>` | 絶対移動（完了待ち）[mm] | `OK` |
| `m<n>P` | 現在位置読み出し [mm] | `<pos>` |
| `m<n>I` / `m<n>i` | ホーミング（即時 / 完了待ち）| `OK` |
| `m<n>NP` / `m<n>NM` | JOG 正 / 負方向開始 | `OK` |
| `m<n>F` | JOG 停止 | `OK` |

**送信例:**

```
mS1.5;m0R2.0\n      → 軸0を+2.0mm相対移動（即時）
mS1.0;m1a-5.0\n     → 軸1を絶対位置-5.0mmへ移動（完了待ち）
m2P\n               → 軸2の現在位置を取得 [mm]
```

### 4.4 レジスタコマンド（`R` プレフィックス）

| コマンド | 説明 | 応答 |
|----------|------|------|
| `RS` | 全パラメータをフラッシュに保存 | `OK` / `NG` |
| `RA` | 全パラメータ読み出し（複数行）| `<addr>:<name>:<unit>:<val>` × 256行 + `END` |
| `RP` | parm[] を内部変数(P_*)へ**即時反映**（再起動なしでリミット等を有効化）| `OK` / `BUSY` |
| `R<addr>S<val>` | 指定アドレスに値を書き込み（RAM のみ、焼き込みなし）| `OK` |
| `R<addr>R` | 指定アドレスの値を読み出し | `<val>` |

- `<addr>`: レジスタアドレス (0〜255)
- `<val>`: 整数または浮動小数点数
- `<unit>`: 0=int, 1=float, 2=char
- **反映の流れ**: `R<addr>S<val>`（RAM書換）→ `RP`（P_* に即時反映, 全軸IDLE必須。動作中は `BUSY`）→ `RS`（フラッシュ保存で次回起動も有効）。
  `Parm_set()` は起動時のみ自動実行のため、`RP` を使わない場合はリセットするまで反映されない。

**`RA` 応答フォーマット（1 行あたり）:**

```
<addr>:<name>:<unit>:<val>\n
```

例（主要パラメータ。詳細は firmware_spec.md のアドレスマップ参照）:
```
0:FIRM_VER   :0:1\n
1:BOX_NO     :0:0\n
2:IS_MASTER  :0:1\n
3:NUM_BOXES  :0:2\n
72:LIM_CW_X   :1:25000\n
88:LIM_CCW_X  :1:0\n
104:COEF_NUM_X:1:1\n
120:COEF_DEN_X:1:1\n
136:START_PPS_X:0:100\n
152:ACCEL_MS_X :0:100\n
168:RESOL_X    :0:1000\n
200:MOTSEL_X   :0:65025\n
...
END\n
```

---

## 4.5 MTD415T TEC 温度コントローラ（`TC` プレフィックス, USART3 ブリッジ）

温度は host 側 °C（float）、デバイス内部は m°C。搭載時のみ有効。

| コマンド | 説明 | 応答 |
|----------|------|------|
| `TC$<raw>` | MTD415T 生コマンドを透過送信（`?` を含めばクエリ）| `OK` / `<応答>` / `NG` |
| `TCT<°C>` | 温度設定値を設定 | `OK` |
| `TCTR` | 温度設定値を読み出し [°C] | `<temp>` |
| `TCM` | 実測温度読み出し [°C] | `<temp>` |
| `TCA` | TEC 電流読み出し [mA] | `<mA>` |
| `TCL<mA>` | TEC 電流制限を設定 | `OK` |
| `TCE` | エラーレジスタ読み出し | `<err>` |
| `TCV` | バージョン読み出し | `<ver>` |

---

## 4.6 汎用 GPIO 出力（`G` プレフィックス）

PE9〜PE12 の 4 点を汎用 GPIO 出力として ON/OFF する。番号 `n` は 0..3（0=PE9, 1=PE10, 2=PE11, 3=PE12）。
ファームは**物理レベル（Low=0 / High=1）**のみを扱い、極性（High=ON / Low=ON）の解釈は GUI 側で行う。

| コマンド | 機能 | 応答 | 補足 |
|----------|------|------|------|
| `G<n>S0` | ピン n を Low 出力 | `OK` | |
| `G<n>S1` | ピン n を High 出力 | `OK` | |
| `G<n>R` | ピン n の物理レベル読み出し | `0` / `1` | High=1 |

- 起動時は CubeMX 既定で全ピン Low。既定極性（High=ON）では Low=OFF＝全 OFF。
- GUI は接続時に全ピンを OFF レベルへ初期化する。

---

## 5. コマンドチェーン

セミコロン `;` で複数のサブコマンドを 1 回の送信にまとめられる。左から順番に実行され、最後に出力を持つサブコマンドの結果が応答として返る。

```
MS1000;M0R500\n
```
↑ `MS1000`（速度設定、応答なし）→ `M0R500`（移動、応答 `OK`）→ ファームが `OK\n` を返す

---

## 6. 動作の即時 / 完了待ち

移動コマンドは 2 種類の動作を持つ。

| 文字 | 動作 |
|------|------|
| 大文字 (`R`/`A`/`I`) | 即時応答：コマンドを受け付けてすぐ `OK` を返す。移動はバックグラウンドで実行。 |
| 小文字 (`r`/`a`/`i`) | 完了待ち：移動が完了してから `OK` を返す。GUI はこの間ブロックされる。 |

---

## 7. エラー応答

| 条件 | 応答 |
|------|------|
| 不明なコマンド | `NG\n` |
| 軸番号が範囲外 | `NG\n` |
| レジスタアドレスが範囲外 | `NG\n` |
| 物理量係数が未設定 (0) | `NG\n` |
| モーター動作中に移動コマンド | 無視（応答は `OK\n`）|
