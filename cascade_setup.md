# カスケード接続 セットアップ手順（2台＝6軸）

複数のステージコントローラを **コントローラ間 SPI（PB3/PB4/PB5）** で連結し、1台の
**マスター機**（PCとUSB通信）から**スレーブ機**の軸を制御する構成の手順書。
まず **2台（マスター1＋スレーブ1＝6軸）** で確実に動かす。

- 軸アドレス: PC/GUI は **グローバル軸番号 0..5**（機番=番号÷3, ローカル軸=番号%3）で扱い、
  ワイヤ上は **`@<box>;<コマンド>`** に変換して送る。
- 機番(BOX_NO): マスター=0, スレーブ=1。
- 詳細なコマンド仕様は [command_spec.md](command_spec.md) の「2.1 カスケード接続時のアドレッシング」を参照。

---

## 0. 前提条件（両機とも）

- [ ] **カスケード対応ファーム**を書き込み済み（v0 / v1 どちらでも可、両方に実装済み）。
- [ ] CubeMX で **SPI1 を有効化**し `MX_SPI1_Init()` を生成済み:
  - ピン **PB3=SPI1_SCK / PB4=SPI1_MISO / PB5=SPI1_MOSI**（AF5）
  - SPI1 = **Full-Duplex Master** / **NSS=Software** / **DataSize=8bit**
  - CPOL/CPHA は**両機で同一**にする（例 CPOL=High, CPHA=2Edge）
  - **NVIC で「SPI1 global interrupt」を有効化**（スレーブ受信に必須）
  - Baud はまず控えめに（例 1〜5 Mbit/s）
- [ ] 2台とも SPI1 の設定を**完全に同一**にする（片方だけ設定違いだと通信不成立）。

---

## 1. 配線（電源OFFで）

同名ピン同士をストレート結線する（マスター＝出力、スレーブ＝入力/出力に自動設定されるため同名でOK）。

```
     マスター機 (BOX_NO=0)              スレーブ機 (BOX_NO=1)
    ┌───────────────┐                 ┌───────────────┐
    │  PB3 (SCK)  ●─┼─────────────────┼─●  PB3 (SCK)   │
    │  PB5 (MOSI) ●─┼─────────────────┼─●  PB5 (MOSI)  │
    │  PB4 (MISO) ●─┼─────────────────┼─●  PB4 (MISO)  │
    │  GND        ●─┼─────────────────┼─●  GND         │  ← 共通GND必須
    │  USB → PC     │                 │  (電源のみ)     │
    └───────────────┘                 └───────────────┘
```

| マスター | スレーブ | 信号 | 向き |
|---------|---------|------|------|
| PB3 | PB3 | SCK（クロック） | マスター→スレーブ |
| PB5 | PB5 | MOSI | マスター→スレーブ |
| PB4 | PB4 | MISO | スレーブ→マスター |
| GND | GND | 共通GND | **必須** |

- **USBはマスター機のみ**PCへ。スレーブ機は電源のみ。
- CS線は使わない（コマンド内アドレスでスレーブを選択）。配線は短く。

---

## 2. 各機の機番設定（USBで1台ずつ・**直接コマンド**）

> 役割は **フラッシュ保存(`RS`)＋リセット後** に有効化される。
> `IS_MASTER` による SPI のマスター/スレーブ切替は **ブート時のみ**実行されるため、
> 設定後は必ず**リセット**すること（`RP`だけではSPIモードは切り替わらない）。

GUI「直接コマンド」欄に、**`@`を付けずに**1行ずつ送る（`@`無し＝自機ローカル実行）。

### 2-1. スレーブ機（機番1）
USBをスレーブ機へ接続し、送信:
```
R2S0      ← IS_MASTER=0（スレーブ）
R1S1      ← BOX_NO=1
R3S2      ← NUM_BOXES=2
RS        ← フラッシュ保存
```
→ 切断 → **スレーブ機をリセット（NRSTボタン or 電源再投入）** → USBを外す。

### 2-2. マスター機（機番0）
USBをマスター機へ接続し、送信:
```
R2S1      ← IS_MASTER=1（マスター）
R1S0      ← BOX_NO=0
R3S2      ← NUM_BOXES=2
RS        ← フラッシュ保存
```
→ 切断 → **マスター機をリセット**。
（新品/初期状態なら既定が IS_MASTER=1/BOX_NO=0/NUM_BOXES=2 のため `RS` のみでも可）

### 設定確認（各機にUSB接続して）
| コマンド | 期待値 | 意味 |
|---------|--------|------|
| `R2R` | マスター=`1` / スレーブ=`0` | IS_MASTER |
| `R1R` | マスター=`0` / スレーブ=`1` | BOX_NO |
| `R3R` | `2` | NUM_BOXES |

---

## 3. カスケード接続してGUIを2台構成に

1. 配線を接続し、両機に電源投入（USBはマスターのみ）。
2. GUIでマスター機に接続。
3. 「設定…」→ **接続台数 = 2** に変更 → OK（保存）。
4. **GUIを再起動**（軸表示を6軸へ反映するため）。
5. 再接続すると `0:X 0:Y 0:Z / 1:X 1:Y 1:Z` の6軸が並ぶ。`1:*` はスレーブ機。

---

## 4. 動作確認（切り分けしやすい順）

GUI「直接コマンド」欄で:
```
@0;M0P              ← マスター軸0の位置（ローカル）→ 数値が返る
@1;M0P              ← スレーブ軸0の位置（中継）→ 数値が返れば往復成功
@1;MS3000;M0R2000   ← スレーブ軸0を相対移動 → スレーブが動く
```
- `@0;…` が返り `@1;…` が `NG` → **カスケード通信が不成立**（下記トラブルシュート）。
- 往復が通ったら、GUIの `1:X` 行のジョグ/移動/原点/位置表示を確認。
- 以後、スレーブの再調整は「設定…」→**機1タブ→「この機に書込・保存」**でマスター経由で可能。

---

## 5. トラブルシュート（`@1;` が NG）

**まず「NGが即返るか / 少し(~0.5秒)待って返るか」を確認**すると切り分けやすい。
- **即NG** → マスター側SPIの問題（マスターがマスターモードでない/SPI1未初期化）。
- **少し待ってNG** → スレーブが応答していない（最有力）。

チェックリスト（上から順に）:

1. **スレーブをリセットしたか**（`IS_MASTER=0` はリセット後に有効）。
   スレーブにUSB直結して `R2R`→`0`, `R1R`→`1` を確認。`R2R`が`1`ならまだマスターモード＝無応答。
2. **スレーブのCubeMXで「SPI1 global interrupt」が有効か**。無効だとスレーブ受信が完了せず永久にBUSY→NG。
3. **配線**: GND共通か。PB3/PB4/PB5 の取り違えがないか。結線が緩んでいないか。
4. **両機のファーム**: 両方ともカスケード対応ファーム＋SPI1生成済みか（片方が旧ファームだと無応答）。
5. **両機のSPI1設定が同一か**: CPOL/CPHA/DataSize が食い違うと受信がCRC不一致→NG。
6. **マスターがマスターか**: マスターにUSB直結して `R2R`→`1`, `R1R`→`0`。
7. **速度を下げる**: SPI1 Baud を 1MHz 以下へ。配線が長い/ノイズが多いと高速で化ける。
8. それでも不安定な場合、フレーム待ちを延ばす（ファーム `functions.c`）:
   - `CASC_RESP_DELAY`（既定2ms）を増やす、`CASC_RESP_RETRY`（既定20）を増やす。

> ヒント: スレーブ機を単体でUSB接続し、ローカルコマンド（`M0P`, `R1R` など）が正しく動くことを
> 先に確認すると、「スレーブ機自体は正常／カスケード配線orSPI設定の問題」と切り分けられる。

---

## 付録: 役割パラメータ（レジスタ）

| アドレス | 名称 | 意味 | 既定 |
|---------|------|------|------|
| 1 | BOX_NO | 機番（マスター=0, スレーブ=1,2,…） | 0 |
| 2 | IS_MASTER | 1=マスター / 0=スレーブ | 1 |
| 3 | NUM_BOXES | 接続台数 | 2 |

- 反映の流れ: `R<addr>S<val>`（RAM書換）→ `RS`（フラッシュ保存）→ **リセット**（役割/SPIモード確定）。
- 3台以上への拡張は、MISO競合回避（対象スレーブのみMISO駆動）が前提。まず2台で確実動作を確認してから。
